/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AngularCopyToClipboardDirective } from './angular-copy-to-clipboard.directive';

describe('Directive: AngularCopyToClipboard', () => {
  it('should create an instance', () => {
    const directive = new AngularCopyToClipboardDirective();
    expect(directive).toBeTruthy();
  });
});
